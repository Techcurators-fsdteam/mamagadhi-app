import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

module.exports = {
  images: {
    domains: [
      'pub-5f2b20e3d1e945a8821922fe1f11631c.r2.dev'
      // add more if needed
    ],
  },
};

export default nextConfig;
